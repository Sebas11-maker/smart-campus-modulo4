{
  "name": "dashboard-service",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "dashboard-service",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo Building dashboard-service"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pytest dashboard-service/test_main.py"
      }
    }
  }
}